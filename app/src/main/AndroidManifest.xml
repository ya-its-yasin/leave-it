<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.leaveit">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name=".AppController"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".FacultyOpenPage"></activity>

        <uses-library
            android:name="org.apache.http.legacy"
            android:required="false" />

        <activity android:name=".StudentTrackRequest" />
        <activity android:name=".StudentHomePage" />
        <activity android:name=".FacultyApprovalPage" />
        <activity android:name=".FacultyHomePage" />
        <activity android:name=".AdminAddStudent" />
        <activity android:name=".AdminAddFaculty" />
        <activity android:name=".AdminViewStudent" />
        <activity android:name=".AdminViewFaculty" />
        <activity android:name=".AdminHomePage" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>